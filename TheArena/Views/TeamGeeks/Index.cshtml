@model IEnumerable<TheArena.Models.TeamGeek>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Geek.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Team1.Initials)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Deleted)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Geek.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Team1.Initials)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Deleted)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeamGeekId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeamGeekId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeamGeekId })
        </td>
    </tr>
}

</table>
